<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        title Document
        // Font Awesome
        link(href='https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.1/css/all.min.css' rel='stylesheet')
        // Google Fonts
        link(href='https://fonts.googleapis.com/css?family=Roboto:300,400,500,700&display=swap' rel='stylesheet')
        // MDB
        link(href='https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/3.2.0/mdb.min.css' rel='stylesheet')

    body
        .input-group
            .form-outline(action="/users/search", method="GET")
                input#form1.form-control(type='text', name="q")
                label.form-label Search
            button.btn.btn-primary(type='button')
                i.fas.fa-search Search
        ul
            each i in admin           
                li= `${i.email} is handsome and age is ${i.password} `                 
                a(href=`/user/${i._id}`) view detail