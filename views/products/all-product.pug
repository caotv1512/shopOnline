<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        title Document
        // Font Awesome
        link(href='https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.1/css/all.min.css', rel='stylesheet')
        // Google Fonts
        link(href='https://fonts.googleapis.com/css?family=Roboto:300,400,500,700&display=swap', rel='stylesheet')
        // MDB
        link(href='https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/3.2.0/mdb.min.css', rel='stylesheet')

    body
        ul
            // ở đâu
            each i in product   
                .card(style='width: 15rem;')
                    img.card-img-top(src=i.image, alt='...')
                    .card-body
                        h5.card-title= i.name 
                        p.card-text=`${i.price}  `
                        p.card-text=`${i.content}  `
                        a.btn.btn-primary(href='#') Go somewhere
                            
                //- .card
                //-     .bg-image.hover-overlay.ripple(data-mdb-ripple-color='light')
                //-         img.img-fluid(src=i.image, width= "480", height="480" )
                //-         a(href='#!')
                //-         .mask(style='background-color: rgba(251, 251, 251, 0.15)')
                //-     .card-body
                //-         h5.card-title= `${i.name}  `
                //-         p.card-text=`${i.price}  `
                //-         p.card-text=`${i.content}  `
                //-         a.btn.btn-primary(href='#!') Mua Ngay
                //-         a(href=`/products/${i._id}`) view detail
