<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        title Document
        // Font Awesome
        link(href='https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.1/css/all.min.css' rel='stylesheet')
        // Google Fonts
        link(href='https://fonts.googleapis.com/css?family=Roboto:300,400,500,700&display=swap' rel='stylesheet')
        // MDB
        link(href='https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/3.2.0/mdb.min.css' rel='stylesheet')

    body
        nav.navbar.navbar-expand-lg.navbar-light.bg-light
            .container-fluid
                nav(aria-label='breadcrumb')
                ol.breadcrumb
                    li.breadcrumb-item
                    a(href='./index.pug') Home
                    li.breadcrumb-item
                    a(href='#') Library
                    li.breadcrumb-item.active(aria-current='page')
                    a(href='#') Data
        h1 Creater Product
        .container
            // làm tiếp 0i, oki babe
            if errors 
                each error in errors
                    .alert.alert-danger= error
            form(method="POST" acction="", enctype="multipart/form-data")
                // muốn gửi file lên thì phải có thêm t cái mã hóa file nữa  , phần này chưa biết , nhớ ra rồi hii
                // thiếu acction
                // Name input
                .form-outline.mb-4
                    // name của input đâu ?,
                    input#form4Example1.form-control(type='text', name = "name", value= values ? values.name : '')
                    label.form-label(for='form4Example1') Name Product
                // Email input
                .form-outline.mb-4
                    input#form4Example2.form-control(type='text'  name = "price", value= values ? values.price : '')
                    label.form-label(for='form4Example2') Price
                // Message input
                .form-outline.mb-4
                    textarea#form4Example3.form-control(rows='4', name = "content", value= values ? values.content : '')
                    label.form-label(for='form4Example3') Content

                // Submit button
                .form-file.mb-4
                    input#customFile.form-file-input(type='file', name = "avatar")
                    label.form-file-label(for='customFile')
                        span.form-file-text Choose file...
                        span.form-file-button Browse
                // đây là không có file gửi lên. thì dùng form bt đc
                // muốn tải file l3n phải có th3m trơờng mã hóa file 0ấy
                // phải dùng th3m 1 cái npm nữa hỗ trợ mã hóa nhé
                button.btn.btn-primary.btn-block.mb-4(type='submit') Add New
        a(href="/products/all-product") View all Product
    script(type='text/javascript' src='https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/3.2.0/mdb.min.js')        